import React from "react";
import ReactDOM from "react-dom/client";
import TipCalculator from "./App.jsx";
import "./styles/index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TipCalculator />
  </React.StrictMode>,
);
